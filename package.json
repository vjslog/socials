{
  "name": "ejay-links",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply ejay_links_db --local",
    "db:migrate:remote": "wrangler d1 migrations apply ejay_links_db --remote"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "wrangler": "^3.90.0",
    "typescript": "^5.6.3"
  }
}
